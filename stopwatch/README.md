# StopWatch example with Apache Commons SCXML

## How to run with Maven

- Move to stopwatch folder
- Run the following:

    $ mvn clean package
    $ mvn exec:java

- You will see a window showing a simple stop watch.
